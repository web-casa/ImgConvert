# Generated Tauri fuzz corpus

`pnpm run fuzz:prepare` writes deterministic seeds for `external_codec_manifest`,
`import_scanner`, and `pdf_document` below this directory. Seed payloads and
local crash artifacts are intentionally ignored; the generator code is the
reviewable source of truth.
